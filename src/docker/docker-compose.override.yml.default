version: "2"

services:

# Override base service.
  php:
    volumes:
# Uncomment line related to you php version to enable mailhog.
      - "./90-mail.ini:/etc/php${PHP_VERSION}/conf.d/90-mail.ini:z"
#      - "./90-mail.ini:/etc/php5/conf.d/90-mail.ini:z"
# Uncomment line related to you php version if you need PHP XDebug.
#    command: php-fpm7 -F -d zend_extension=xdebug.so
#    command: php-fpm5 -F -d zend_extension=xdebug.so

#  adminer:
#    image: dockette/adminer:mysql-php7
#    container_name: "${COMPOSE_PROJECT_NAME}_adminer"
#    links:
#      - mysql:mysql
#    depends_on:
#      - mysql
#    networks:
#      - front

  mailhog:
    image: skilldlabs/mailhog
    container_name: "${COMPOSE_PROJECT_NAME}_mail"
    restart: always
    networks:
      - front

# Mount local folder with ssl keys.
#  nginx:
#    volumes:
#     - ./nginx/ssl:/etc/nginx/ssl:Z
